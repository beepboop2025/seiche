"""The Gyre — Takens delay embedding + empirical dynamic modeling.

PLACEHOLDER (v2.6 build in progress): the full engine — simplex-projection
skill by horizon, the phase-randomized surrogate determinism gate, the S-map
nonlinearity test and the local stability spectrum — lands in the next
commit. Until then the engine reports itself down, the house way: fail-loud,
never a silent gap.
"""

from __future__ import annotations

import pandas as pd


def analyze(spread_bp: pd.Series) -> dict:
    return {"ok": False, "reason": "engine under construction (v2.6 build in progress)"}
